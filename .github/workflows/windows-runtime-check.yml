name: Windows Runtime Check

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "kapsl-runtime/**"
      - ".github/workflows/windows-runtime-check.yml"
  push:
    branches:
      - main
    paths:
      - "kapsl-runtime/**"
      - ".github/workflows/windows-runtime-check.yml"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  windows-runtime-check:
    name: Cargo check (Windows runtime)
    runs-on: windows-latest
    env:
      CARGO_NET_GIT_FETCH_WITH_CLI: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Git auth for private SDK dependency
        shell: pwsh
        run: |
          git config --global url."https://x-access-token:${{ secrets.SDK_REPO_ACCESS_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cargo check runtime CLI
        shell: pwsh
        run: |
          cargo check --manifest-path kapsl-runtime/Cargo.toml --target x86_64-pc-windows-msvc -p kapsl

      - name: Cargo check runtime benchmarks
        shell: pwsh
        run: |
          cargo check --manifest-path kapsl-runtime/Cargo.toml --target x86_64-pc-windows-msvc -p shm-bench-native -p native-hybrid-client
